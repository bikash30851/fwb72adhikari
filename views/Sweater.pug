extends layout

block content
  h1= title
  - const Sweater = ['color','price','size']
  - let results;
  div(class='container-fluid')
    div(class='row')
      each results, index in Sweater 
        div(class='col-sm-2') #{results}